# ... (content of the file app/models/pb_core_presenter.rb up to line 294)

      # raise('Should not have "Level of User Access" annotation if not digitized') if !digitized? && access_levels.count != 0.

# ... (content of the file app/models/pb_core_presenter.rb after line 295)