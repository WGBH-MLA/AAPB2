<% if @exhibit %>
  <%= render partial: 'exhibits/show_all_items_header', locals: {exhibit: @exhibit } %>
<% end %>

<%= render 'did_you_mean' %>

<%= render 'constraints' %>

<%= render 'sort_and_per_page' %>