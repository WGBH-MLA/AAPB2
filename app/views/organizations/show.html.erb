<div class="page-header">
  <h1>Participating Organizations</h1>
</div>



<br />

<div class="row">
  <!-- START: Main Column -->
  <div class="col-md-12">

    <!-- Org-o-tron -->
    <div class="jumbotron org-o-tron">
      <div class="container">

        <h2><%= @org.short_name %></h2>

        <p><%= @org.city %>, <%= @org.state %></p>

        <% unless @org.logo_src.empty? %>
          <img src="<%= @org.logo_src %>" alt="<%= @org.short_name %> logo" class="org-logo"/>
        <% end %>

        <%# TODO: better link idiom %>
        <p><a href="<%= @org.url %>"><%= @org.url %></a></p>

        <a href="/catalog?f[contributing_organizations][]=<%= CGI::escape(@org.facet)%>" class="btn btn-default btn-sm">View All Records</a>

      </div>
    </div>
    <!-- END Org-o-tron -->

    <% unless @org.about_html.empty? %>
      <a name="about"></a>
      <div class="panel panel-default">
        <div class="panel-heading">
          <h2 class="panel-title">About</h2>
        </div>
        <div class="panel-body">
          <%= @org.about_html.html_safe %>
        </div>
      </div>
    <% end %>

  </div>
  <!-- END: Main Column -->

</div>
